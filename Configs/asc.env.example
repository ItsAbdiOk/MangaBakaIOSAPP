# Copy to Configs/asc.env (gitignored) and fill in.
# Neither of these is a secret — they are identifiers. The actual private key
# stays at ~/.appstoreconnect/private_keys/AuthKey_<KEY_ID>.p8 and is never read
# by anything that prints.
ASC_KEY_ID=
ASC_ISSUER_ID=
