// Base build settings shared by all configurations.
//
// Settings shared by both configurations. The personal access token is NOT
// included here — it belongs to Debug.xcconfig only, so no Release build can
// carry it. The app works without a token either way: every discovery endpoint
// is public (51 of 77 operations need no auth).
MB_API_BASE_URL = https:/$()/api.mangabaka.org
